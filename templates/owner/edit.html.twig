{% extends 'base.html.twig' %}

{% block title %}Edit Owner
{% endblock %}

{% block main %}
	<h1>Edit Owner</h1>

	{{ include('owner/_form.html.twig', {'button_label': 'Update'}) }}

	{{ include('owner/_delete_form.html.twig') }}

	<a href="{{ path('app_owner_index') }}" class="btn btn-secondary" style="margin-top:10px">back to list</a>
{% endblock %}
